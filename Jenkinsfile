pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello from Jenkins Pipeline 👋'
            }
        }

        stage('Build') {
            steps {
                echo 'Build step running...'
            }
        }

        stage('Done') {
            steps {
                echo 'Pipeline finished successfully ✅'
            }
        }
    }
}